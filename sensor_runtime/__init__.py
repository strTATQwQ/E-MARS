"""Model-free, fail-closed sensor bootstrap and soak runtime.

The package intentionally has no eager imports so lifecycle recovery remains
usable from a minimal host Python even when Isaac/ROS dependencies are absent.
"""
