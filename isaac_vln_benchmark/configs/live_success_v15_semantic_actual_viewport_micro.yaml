benchmark:
  name: live_success_v15_semantic_actual_viewport_micro
  timeout_sec_default: 90
  seeds: [0]
  modes:
    - step_semantic_actual_viewport_v15_micro

tasks:
  semantic_target:
    ids: [semantic_micro_clear]
    count: 1
    timeout_sec: 90

evidence_policy:
  require_real_step_http: true
  require_multimodal_step_roles: [semantic_stop]
  require_distinct_frame_seq: true
  required_track_hits: 2
  allow_mock_or_fallback: false
  qualification_evidence: false
  locomotion_fidelity: ideal_kinematic
  image_domain: actual_isaac_viewport
