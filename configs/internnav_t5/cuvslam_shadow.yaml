/**/internvla_go2_controller_bridge:
  ros__parameters:
    # T5-only opt-in: publish the independent 320x240 stereo pair while GT
    # remains the sole navigation pose source during shadow evaluation.
    enable_stereo_feed: true
    pose_source: ground_truth
    use_sim_time: true

/**/visual_slam_node:
  ros__parameters:
    use_sim_time: true
